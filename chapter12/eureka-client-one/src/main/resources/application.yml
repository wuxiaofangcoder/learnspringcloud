eureka:
  client:
    service-url:
      defaultZone:
        http://localhost:8761/eureka/
server:
  port: 8762
spring:
  application:
    name: eureka-client-one
logging:
  file: "logs/eureka-client-one.log"
management:
  security:
    enabled: false